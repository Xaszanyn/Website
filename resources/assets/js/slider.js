function slide(slider)
{
    for (i = 1; i <= slider.dataset.volume; i++)
    {
        alert(i);
        //let image = document.createElement("img");
        //image.src = `${slider.dataset.address}/`
    }
}